__pycache__/
*.pyc
.pytest_cache/
.venv/
.claude/settings.local.json

# OpenSky API client secret. Belongs in GitHub Actions secrets, never in git.
credentials.json
.env

# data/gulfwatch.db is committed on purpose -- git is the history layer.
# The WAL sidecars are not.
data/*.db-wal
data/*.db-shm
